import React from 'react'
export default function AdminRooms(){ return <div className="card">Add an admin UI here to CRUD hotels/room types and set availability via API endpoints under /api/admin/*</div> }
